{% if sectionStarted %}
    </section>
    <!-- section ended -->
{% endif %}
<!-- section start -->
<section class="contents col-100" {% if block.elementId %} id="{{ block.elementId }}"{% endif %}>
    {% set sectionStarted = true %}

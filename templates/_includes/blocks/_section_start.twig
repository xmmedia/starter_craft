{% include '_includes/blocks/_element_id.twig' %}

{% if sectionStarted %}
    </section>
    <!-- section ended -->
{% endif %}
<!-- section start -->
<section class="contents col-100" {% if elementId %} id="{{ elementId }}"{% endif %}>
    {% set sectionStarted = true %}

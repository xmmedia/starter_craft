<div class="gallery-wrap {{ block.classes }}"{{ blockId(block) }}>
    {% for galleryImage in block.gallery.all() %}
        {% if galleryImage %}
            {{ image(galleryImage.galleryImage.one(), 'gallery') }}
        {% endif %}
    {% endfor %}
</div>
